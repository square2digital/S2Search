namespace S2Search.Backend.Domain.Constants;

public static class CacheKeys
{
    public const string DefaultFacets = nameof(DefaultFacets);
    public const string ClientTheme = nameof(ClientTheme);
    public const string QueryCredentials = nameof(QueryCredentials);
    public const string Synonyms = nameof(Synonyms);
}
